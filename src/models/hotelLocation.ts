export class HotelLocation{
    public locationId: string
    public name: string
    public code: string
    public type: string
    public country: string
    public countryCode: string
    public city: string
    
    constructor(
    ) { }
}